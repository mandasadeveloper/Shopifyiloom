<div id="react-store" data-color="blue" data-item="1123"></div>

<div class="container">
  <div class="page-spacer">
    <div class="section">
      <h1 class="h1 text-center">{{ page.title }}</h1>

      {%- if page.content != empty -%}
        <div class="prose">
          {{ page.content }}
        </div>
      {%- endif -%}
    </div>
  </div>
</div>